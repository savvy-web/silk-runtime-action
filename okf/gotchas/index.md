# Gotcha

* [A `Closes #N` trailer merged into `dev` does not close the issue](closes-does-not-fire-on-dev-merge.md) - Why a PR body's Closes trailer silently fails to auto-close its issue when the PR merges into dev instead of main, and what to do instead.
* [A green install after unlinking a dogfooded package does not mean the code still typechecks](warm-store-masks-unlinked-typecheck.md) - Why a same-version, different-content file colon link can leave pnpm install green and the typecheck red, and why a warm pnpm store hides it locally.
* [A green tsc, Biome, unit suite and two reviews still shipped a dead bundle](minifier-folds-escaped-templates.md) - A shell-interpolation string escaped as a template literal passes every check that runs against source and dies at module load in the minified dist.
* [A hand-written test double is more permissive than the runner it replaces](in-memory-doubles-are-more-permissive.md) - Why a green suite over a Map-backed or hand-stubbed service double can hide a bug the real runner reports on every run.
* [A workflow trigger filter that matches no branch fails open into silence](branch-filter-fails-open.md) - A typo'd branch name in an on.pull_request.branches filter produces no error, no skipped job and no annotation — the matrix simply never runs against that branch.
* [An "exact hit" that installs from the network anyway is a poisoned cache, not a fast one](exact-hit-skips-save-poisoning.md) - A workspace or store cache entry archived empty under a shared key reports exact hit forever after, and every later run pays a full install while believing it saved one.
* [`Secret.adopt` looks like the right member for the turbo server's config reader, and is wrong twice over](secret-adopt-looks-like-the-right-fit.md) - Why the kit's Secret.adopt is the wrong fit for readServerConfig even though its shape matches the detached worker's Redacted.make calls exactly.
* [`addPath` looks like it updates `PATH`; it only reaches later steps](add-path-is-for-later-steps.md) - Reading ActionOutputs.addPath as an immediate PATH mutation instead of a GITHUB_PATH append that later workflow steps pick up.
* [`defaults.run.working-directory` does not reach a `uses:` step](uses-step-ignores-working-directory.md) - A composite or Docker uses step always executes at GITHUB_WORKSPACE regardless of a job's defaults.run.working-directory, so a fixture-scoped action step installs the wrong repository's dependencies unless told not to.
